export interface Product {
  productCode?: string;
  productName?: string;
  productImage?: string[];
  productPrice?: number;
  productDescription?: string;
  productCategory?: string;
  productContent?: string;
}
